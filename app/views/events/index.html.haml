
%table.table
  %thead
    %tr
      %th Title
      %th Description
      %th Max Volunteers
    %tbody
    - @events.each do |event|
      %tr
        %td= event.title
        %td= event.description
        %td= event.max_volunteers
